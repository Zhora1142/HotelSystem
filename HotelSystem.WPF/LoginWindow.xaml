<Window x:Class="HotelSystem.WPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HotelSystem - Авторизация" Height="220" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Логин:" Margin="0,0,10,12" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" Margin="0,0,0,12"
                 Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Text="Пароль:" Margin="0,0,10,12" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2">
            <TextBlock Text="Администратор: admin / admin" Foreground="Gray" Margin="0,0,0,4"/>
            <TextBlock Text="Тестовый клиент: sokolova / 123" Foreground="Gray" Margin="0,0,0,12"/>
            <Button Content="Войти"
                    Width="110"
                    HorizontalAlignment="Center"
                    IsDefault="True"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Grid>
</Window>
